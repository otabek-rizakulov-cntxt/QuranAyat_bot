[pytest]
# asyncio_mode = auto lets plain `async def test_*` run without a per-test marker.
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra
